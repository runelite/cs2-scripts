// 433
[clientscript,grouping_dropdown](int $int0)
sound_synth(interface_select1, 1, 0);
if ($int0 = 1) {
	if_sethide(false, grouping:dropdown);
} else {
	if_sethide(true, grouping:dropdown);
}
~grouping_rebuild;
