// 8331
[clientscript,script8331](component $component0, int $comsubid1, int $int2, string $string0)
if (clientclock >= $int2 & cc_find($component0, $comsubid1) = ^true) {
	cc_settext($string0);
	cc_setontimer(null);
}
