// 9402
[proc,script9402](dbrow $dbrow0)(string)
def_string $string0 = db_getfield($dbrow0, talent_tree:name, 0);
def_int $int1 = db_getfield($dbrow0, talent_tree:effect, 0);
if_query(1, talent_tree:hotfix, -1);
while (if_query_next = true) {
	if (string_length(cc_param(param_2570)) > 0 & cc_param(param_2569) = $int1) {
		return(cc_param(param_2570));
	}
}
return($string0);
