// 6580
[clientscript,toa_speedrun_time_update](int $int0, int $int1)
if ($int0 = -1) {
	if_setontimer(null, toa_hud:raid_timer);
	if_settext("-", toa_hud:raid_timer);
} else {
	$int0 = calc($int0 * 30 - clientclock);
	if ($int1 = 1) {
		~toa_time_update_timer(toa_hud:raid_timer, $int0, %varbit11866, %varbit11890);
		if_setontimer(null, toa_hud:raid_timer);
	} else {
		if_setontimer("toa_time_update_timer(toa_hud:raid_timer, $int0, %varbit11866, %varbit11890)", toa_hud:raid_timer);
	}
}
