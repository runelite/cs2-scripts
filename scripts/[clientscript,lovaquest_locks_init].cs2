// 2736
[clientscript,lovaquest_locks_init](component $com_lovaquest_locks_lock1_light0, component $com_lovaquest_locks_lock2_light1, component $com_lovaquest_locks_lock3_light2, component $com_lovaquest_locks_lock4_light3)
if (%varbit7797 < 4) {
	if_setmodel(model_36241, $com_lovaquest_locks_lock1_light0);
} else {
	if_setmodel(model_36244, $com_lovaquest_locks_lock1_light0);
}
if (%varbit7798 < 4) {
	if_setmodel(model_36241, $com_lovaquest_locks_lock2_light1);
} else {
	if_setmodel(model_36244, $com_lovaquest_locks_lock2_light1);
}
if (%varbit7799 < 4) {
	if_setmodel(model_36241, $com_lovaquest_locks_lock3_light2);
} else {
	if_setmodel(model_36244, $com_lovaquest_locks_lock3_light2);
}
if (%varbit7800 < 2) {
	if_setmodel(model_36241, $com_lovaquest_locks_lock4_light3);
} else {
	if_setmodel(model_36244, $com_lovaquest_locks_lock4_light3);
}
